Think english speak russian.

Repository directory: $repo_dir

Make a report on last changes in code and git history. Prefer concrete technical description over abstract improvements or broad conclusions.

Break down changes for commit list from push payload: $commit_ids
Use only these commits from payload. Do not calculate commit ranges from before/after or describe changes outside these payload commits.
If payload commit list is empty, treat it as branch deletion or branch ref update without commits. Say that directly and do not invent a commit range.

For every commit, summarize briefly what was changed (no code citation, no excessive details, keep in 50-70 words per commit or less). No grand summarization. 

When making the summary you can traverse more code than you will get directly from diffs for better understanding and clearer description.
If one of the payload commits is a merge commit, do not retell every merged commit description. Mention the merge briefly only when it adds new information not already covered by the branch commits.

Put output to $output_path, replace it if exists. Make some output even when you fail during analysis.

Specify in header (1 line): branch name ($branch_name). Nothing extra.

Use short commit numbers, add authors.

Format beautifully for TG.
